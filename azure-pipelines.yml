parameters:
- name: azureServiceConnection
  type: string
  default: iiot
- name: azureContainerRegistry
  type: string
  default: johanngdevacr

stages:
#- stage: build
#  displayName: 'Build and Test Code'
#  jobs:
#  - template: tools/templates/ci.yml
- stage: images
  displayName: 'Build Images'
#  dependsOn:
#  - build
  jobs:
  - template: tools/templates/acrbuild.yml
